# אודיט אב־טיפוס — Tickets V6

**תאריך:** 2026-08-02  
**סוג:** אב־טיפוס סטטי (HTML/CSS/JS) · RTL · ללא backend  
**מטרה:** סקירת מצב נוכחי — מה קיים, מה עובד, ומה מוגבל בכוונה

---

## 1. סיכום מנהלים

הפרויקט הוא אב־טיפוס אינטראקטיבי מלא יחסית לזרימת רכישת כרטיסים: מדף הבית ועד אישור הזמנה, כולל בחירת מושבים באולם, אזור אישי ומצב הדגמה.  
העיצוב מופשט בכוונה (לפי מסך ההקדמה). הנתונים מדומים; אין שרת או תשלום אמיתי.

**סטטוס כללי:** מוכן לסקירת UX / דמו פנימי.

---

## 2. נקודות כניסה ומסכים

| מסך / קובץ | סטטוס | הערות |
|------------|--------|--------|
| `intro.html` | ✅ | הקדמה לסקירה |
| `index.html` | ✅ | דף בית מאושר + כל מסכי ה־SPA |
| כל המופעים | ✅ | סינון טווח תאריכים + טעינה נוספת |
| עמוד מופע | ✅ | מועדים / מידע, מכירה מוקדמת, תור, המתנה |
| בחירת מושבים | ✅ | מפת אולם, זום, מסך מלא, סיכום |
| סל → פרטים → תשלום → הצלחה | ✅ | זרימה מדומה |
| אזור אישי | ✅ | כרטיסים, מועדפים, הזמנות, פרופיל |
| יצירת קשר | ✅ | טופס מדומה |

---

## 3. יכולות שנוספו / חוזקו לאחרונה

### מובייל ורספונסיביות
- תפריט המבורגר (מסך מלא כהה) מתחת ל־960px
- פאנל תאריכים בחיפוש נפתח ימינה מתחת ל־900px (ללא דחיפת מסך)
- שורות מועדים נערמות מתחת ל־1200px; ספירה לאחור נשארת קומפקטית
- חיצי קרוסלה מוסתרים מתחת ל־900px; מעל — chevron מעודכן + disabled בקצוות
- מבנה אולם נשמר מתחת ל־1200px (גלילה/זום, בלי ערימת עמודות)

### בחירת מושבים
- שמות במות: במה ימין / מרכזית / שמאל — צמודות לתרשימים
- מסך מלא: תיקון stacking / כפתור «בחירה» מתחת ל־1100
- במצב הדגמה: סמן נשאר גלוי (z-index / העברה ל־fullscreen)

### זרימות
- תור (זמינות נמוכה): לחיצה על רקע ריק מדלגת לבחירת מושבים
- «אזור אישי» בכותרת מנווט לאזור האישי

---

## 4. ארכיטקטורה קצרה

```
intro.html
    └── index.html  (מסכי SPA בתוך אותו מסמך)
            ├── css/styles.css (+ home2/3, seats-hall, demo-mode)
            └── js/app.js (+ seats-hall.js, demo-mode.js, home2-hero.js)
```

- ניתוב: `location.hash` (`#home`, `#event/dates`, `#account/tickets`, …)
- מצב: משתנים ב־`app.js` + `localStorage` (מועדפים, סל)
- אין bundler / framework

---

## 5. מגבלות ידועות (מכוונות / טכניות)

| נושא | פירוט |
|------|--------|
| אין backend | הכל client-side; אין התחברות אמיתית |
| תשלום | מדומה בלבד |
| עיצוב | לא סופי — לפי הצהרת `intro.html` |
| נתוני מופעים | מערך סטטי ב־`app.js` |
| תמונות מופעים | placeholders / הירו חלקי |
| נגישות | בסיסית (aria על חלקים); לא נבדק WCAG מלא |
| דפדפנים | מיועד לדפדפנים מודרניים; RTL scroll עשוי להשתנות בין מנועים |

---

## 6. בדיקות מומלצות לסקירה

1. `intro.html` → דף בית → חיפוש → רשימת מופעים  
2. מופע עם זמינות גבוהה → בחירת מושבים → סל → רכישה  
3. מופע זמינות נמוכה → תור → דילוג בלחיצה על רקע  
4. מופע אזלו → רשימת המתנה  
5. מכירה מוקדמת / ספירה לאחור  
6. מובייל (&lt;960): המבורגר, חיפוש תאריך, סיכום מושבים  
7. מצב הדגמה + מסך מלא באולם  
8. אזור אישי + מועדפים + כרטיסים דיגיטליים  

---

## 7. המלצות המשך (לא חוסם דמו)

1. הפרדת נתונים לקובץ JSON  
2. בדיקת נגישות מקצה לקצה (מקלדת, קורא מסך)  
3. חיבור API אמיתי כשיהיה מוכן  

---

## 8. מסקנה

האב־טיפוס מכסה את מסע המשתמש המרכזי לרכישת כרטיסים, כולל מובייל, אולם ישיבה ומצב הדגמה.  
מתאים לסקירת UX ולאיסוף משוב לפני שלב העיצוב הוויזואלי.
